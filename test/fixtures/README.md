# HTML fixtures for field detection

Drop real-world form HTML snapshots here (Greenhouse, Ashby, Teamtailor, Form.io).

Future: load with a DOM implementation and call `afCollectFields()` / resume detection.

For now, pure helpers are covered in `test/run.js` without jsdom.
